packages:
  - 'apps/*'
  - 'packages/*'

# O esbuild precisa do script de instalacao para baixar o binario da plataforma.
# E dependencia do Vite, que o Nuxt usa. Sem isso o build do front nao roda.
onlyBuiltDependencies:
  - esbuild
